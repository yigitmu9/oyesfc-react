import React from 'react';
import SliderCard from "../components/SliderCard";

const MainPage = () => {
    return (
        <div>
            <SliderCard/>
        </div>

    );
};

export default MainPage;
